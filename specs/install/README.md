# WSL4AI — installer specifications

Documentation for the **bootstrap installer** (`install/install.sh`). **`defaults.env`** is **`source`d** next to **`install.sh`** or **downloaded** by the script from GitHub raw if missing. CLI/TUI product specs live under [`../tool/`](../tool/).

| Document | Contents |
| -------- | -------- |
| [installer.md](installer.md) | Full installer behaviour: prerequisites, prompts, steps, `local.env`, download flows. |

The implementation is authoritative; this tree describes intended behaviour and operator steps.
